# Hue Secrets Template
# Feature: 002-hue-integration
# Date: 2025-11-18
#
# INSTRUCTIONS:
# 1. Copy this file to /config/secrets.yaml (repository root)
# 2. Replace placeholder values with your actual Hue Bridge credentials
# 3. NEVER commit secrets.yaml to version control (already in .gitignore)
#
# How to get these values:
# - api_key: Generated during first-time authentication (button press flow)
# - bridge_id: Automatically retrieved during authentication
#
# Run authentication: python source/collectors/hue_auth.py

hue:
  # Hue Bridge API key
  # Generated during authentication (requires pressing button on Bridge)
  api_key: "YOUR_HUE_API_KEY_HERE"
  
  # Hue Bridge unique identifier
  # Automatically discovered during authentication
  bridge_id: "YOUR_BRIDGE_ID_HERE"

# Example (DO NOT USE - these are fake credentials):
# hue:
#   api_key: "abc123def456ghi789jkl012mno345pqr678stu901vwx"
#   bridge_id: "001788fffe100200"
